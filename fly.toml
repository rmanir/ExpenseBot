app = "expensebot"
primary_region = "sin"   # Closest to India

[build]
  dockerfile = "Dockerfile"

[[services]]
  internal_port = 8080
  processes = ["app"]

  [services.concurrency]
    type = "requests"
    hard_limit = 20
    soft_limit = 10
